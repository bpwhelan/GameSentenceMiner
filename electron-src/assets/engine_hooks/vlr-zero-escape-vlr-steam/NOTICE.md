# Notice

This support package contains no copied character tables, decoder resources, or
third-party binary assets.

The text-builder signatures, UTF-8 register usage, `<K>`/`<P>` dialogue
terminators, and advance behavior extend the observations in the installed GSM
Agent script:

`%APPDATA%/GameSentenceMiner/agent-scripts/scripts/PC_Steam_Zero_Escape_The_Nonary_Games_Virtue's_Last_Reward.js`

That Agent script is used here as a reverse-engineering reference only. The
packaged payload is a standalone GSM Frida payload and does not load, import, or
depend on Agent. GSM project code is licensed under the repository's
`LGPL-3.0-only` license.
